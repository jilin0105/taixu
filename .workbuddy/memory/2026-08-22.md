# 2026-08-22

## Android 核心环境装配架构重构（android-core 插件）

用户诉求：SDK 不应在构建脚本里现场下载（浪费时间），应前移到插件装配期；下载优先国内镜像；构建脚本里的兼容性操作也应进插件。

改动：
- `app/src/main/assets/scripts/setup_android_core.sh` 重写：插件装配期一次性部署真实 Android 34 平台包（platform-34-ext7_r03.zip，腾讯云镜像 mirrors.cloud.tencent.com/AndroidSDK/，SHA-1 校验）+ Gradle 8.9（腾讯云→华为云，SHA-256 d725d707...cab 校验）+ 全套 6 种 licenses + build-tools 软链（aapt/zipalign/apksigner/aidl）+ 阿里云 Maven init.gradle + /etc/profile.d/taixu-android.sh 持久化环境变量。幂等可重入。
- `build_android.sh` 瘦身为纯执行器：加载 profile → 快速失败校验 → local.properties → 多级 Gradle 调度，无任何下载。
- `android` CLI（assets/tools/android）：PATH/doctor 升级 8.9；`init` 脚手架预置阿里云镜像；`build` 委托统一构建引擎；doctor 增加 android.jar 检查。
- `template_gradlew.sh` / `WorkspaceManager.kt` / `WorkspaceSampleSeeder.kt`：Gradle 8.9 优先（8.7 兜底），wrapper distributionUrl 升 8.9。
- `DevEnvironmentSuite.kt`：android-core 增加 apksigner 包；checkCommand 校验真实 SDK（java+aapt+android.jar+gradle launcher）；apt 聚合安装失败降级 --ignore-missing。
- `ToolManager.kt`：重型下载脚本超时 180s→20min（HEAVY_SETUP_STEP_TIMEOUT_MS），进度文案更新。
- `AgentSkill.kt` / `README.md` / `docs/KNOWN_ISSUES.md` §7：同步文档。

验证：:core:model / :tools / :runtime compileReleaseKotlin BUILD SUCCESSFUL；5 个 shell 脚本 sh -n 通过；腾讯镜像平台包 URL HEAD 200，Content-Length 63180081 与官方清单一致。

宿主机构建注意：Git Bash 环境无 JAVA_HOME，需 `JAVA_HOME="C:/Users/wkbin/.jdks/jbr-17.0.14"` 再跑 gradlew.bat。
