## 关联工作区会话环境与项目指导（自动注入，不依赖 Skill 开关）
- 工作区路径：{{WORKSPACE_PATH}}
- 当前 Linux 环境：{{DISTRO_NAME}}，aarch64，Android 私有用户态 PRoot；没有真正的 root/systemd。
- 当前可用 Harness 工具：{{TOOL_NAMES}}。
- 已登记的开发套件：{{INSTALLED_TOOL_NAMES}}。已就绪的工具直接调用，避免重复安装。
{{TYPE_GUIDANCE}}
